[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[tool.maturin]
# `pre` builds the preprocessor (parquet + manifest.yaml -> rkyv) into the
# python module, exposing `rustler.preprocess`.
features = ["pyo3/extension-module", "pre"]

